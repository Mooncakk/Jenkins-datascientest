pipeline {
	agent any
    environments {
    	toto_key = credentials('toto_secret')
    }
    stages {
    	stage ('test secret') {
        	steps {
            	sh 'echo token $toto_key'
            	sh 'query {
                        repository(owner: "mooncakk", name: "jenkins-datascientest") {
                            pullRequest(number: 13) {
                                id
                            }
                        }
                }'

            }
        }
    }
}